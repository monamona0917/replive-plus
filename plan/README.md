# 计划索引

这个目录用于维护 AI agent 在本仓库里的执行计划。

## 当前活跃计划

- `chat-ui-multi-room-cursor-translation.md`：聊天界面改造的历史计划，当前实现位于 `replive-web-pro/`；其房间切换、游标式历史消息分页、翻译按钮和原始媒体 URL 展示目标已作为现有功能的背景资料保留。
- `project-handoff-2026-08-26.md`：当前交接说明，记录启动摘要、聊天/媒体统计、直播监控和 FFmpeg 录像恢复策略。

这些文件是当前交接文档，不是一次性草稿。行为、协议、数据库、媒体、轮询或构建方式发生变化后，应同步更新“当前实现”和“待验证”部分；源码现状优先于历史计划文字。

## 历史上下文

- `legacy-maintenance-login-plan.md`：从旧的根目录 `plan.md` 中整理出来的历史上下文，包括后端稳定性修复、登录行为、token 刷新失败处理、纯 Go SQLite、panic 日志等。
- `project-handoff-2026-08-20.md`：2026-08-20 的历史交接快照，包含直播监听合并、日志策略、工作区状态和构建约定。
- 原始未跟踪的根目录 `plan.md` 仍保留在原位。除非用户要求，不要删除或覆盖。

## 执行策略

- 先读 `agents.md`。
- 再读当前活跃计划。
- 涉及 chat UI 的新需求先更新计划中的范围和验收点；用户明确要求实施后再修改源码。
- 如果需求范围变化，先更新对应计划文件，再继续实现。
- 实现过程中新增注释时优先使用中文。
